/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package data.streaming.prototypes.app;

import data.streaming.prototypes.list.LinkedList;

import static data.streaming.prototypes.utilities.StringUtils.join;
import static data.streaming.prototypes.utilities.StringUtils.split;
import static data.streaming.prototypes.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
